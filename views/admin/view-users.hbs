<section>
    <div class="container">
        <div class="row mt-4">
            <a href="/admin/add-user" class="btn btn-success ml-auto">Edit</a>

        </div>
        <table class="table mt-4">
            <thead>
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">User</th>

                    <th scope="col">email</th>

                </tr>
            </thead>
            <tbody>
                {{!-- {{#each spots}}
    <tr>
      <th scope="row">1</th>
      <td>{{this.place}}</td>
      <td>{{this.category}}</td>
      <td>{{this.description}}</td>
       <td><img src="{{this.image}}" alt=""></td>
    </tr>
    {{/each}} --}}

            </tbody>
        </table>
    </div>
</section>